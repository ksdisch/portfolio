# Decisions

| ID | Decision | Status | Date | Source/Rationale |
|----|----------|--------|------|-----------------|
| D1 | Present the portfolio method-first, with `dim-stage` as the lead card and `forge-gap` as the most legible artifact | Approved | 2026-07-21 | README.md — dim-stage is the highest-status, most defensible surface; forge-gap's chart is graspable in five seconds |
| D2 | Exclude `reclaim-eval` and `jacobian-lens` from project cards | Approved | 2026-07-21 | README.md, GAPS-AND-NEXT.md — they are the paper authors' reference packages used as cross-check oracles, not Kyle's work; the line is drawn cleanly for skeptics |
| D3 | Make `blind-cite` public, completing all-six-public | Superseded by D6 | 2026-07-21 | GAPS-AND-NEXT.md — last private repo; flipping it removed the only sharing blocker |
| D4 | Copy no code into this folder; every card links out to its real repo, and every stat is verified against that repo | Approved | 2026-07-21 | README.md — keeps the portfolio honest and lets a reader run the actual work |
| D5 | Move the fluency curriculum (LEARNING-ROADMAP.md, PRACTICE.md, `learn/`) to the private `ksdisch/portfolio-prep` | Approved | 2026-07-28 | It is study material about my own gaps. Publishing "which parts I can't yet defend" and "which objections I've rehearsed" is the opposite signal from the calibrated confidence the rest of the portfolio earns — and it sat in the root of the repo recruiters are steered toward. Reverses part of the original build's assumption, not D3 |
| D6 | Park `blind-cite` private at M0 rather than leave it public mid-contradiction | Superseded (by D12) | 2026-07-28 | Its README said "M0 not yet started" while `PROJECT.md` reported M0 complete, and it referenced `m1.py`–`m3.py` that don't exist. Supersedes D3 until the M1 decision lands |
| D7 | Card `mute-map` | Approved | 2026-07-28 | It was public, complete (M0–M3), and absent from the entire index — the one project the portfolio's own "full set" table silently omitted |
| D8 | Record the `jacobian-lens` fork deletion: all 10 commits, 97 study files, and 9 feature branches mirrored to the private `ksdisch/jacobian-lens-study`, then the public fork deleted | Approved | 2026-07-28 | GitHub audit pass — the fork carried Anthropic's package plus ~8 MB of re-hosted paper figures under Kyle's account and made the profile language bar read HTML-first; GitHub cannot make a public fork private, so the supported path was mirror-to-duplicate, verify, then delete. Extends D2's line: reference oracles are never presented as Kyle's work. Recorded retroactively 2026-08-01 |
| D9 | Restructure the index into two lanes — Lane 1: model internals (the J-lens lineage `dim-stage` → `mute-map` → `hush-gauge`); Lane 2: agent reliability (behavioral reproductions) | Approved | 2026-08-01 | README.md — the internals projects share one instrument, one arc (build it → map with it → audit with it), and an epistemic profile distinct from paper reproductions; one flat table hid that. D1 is preserved: dim-stage still leads, forge-gap is still the most legible artifact, one "read first" from each lane |
| D10 | Card `hush-gauge` while in progress (M0/G0 only), and refresh `mute-map`'s card for the M4 close-out | Approved | 2026-08-01 | projects/hush-gauge.md, projects/mute-map.md — D7's lesson ran one direction (a complete public project sat uncarded); this extends it to the other: the index shouldn't hide an active public project either. The in-progress card claims nothing beyond its one decided gate |
| D11 | Card `hush-gauge`'s M1 (G1/G2) in full now; carry M2/G3 as status only — no M2 stats in any table — until hush-gauge PR #10 merges | Approved | 2026-08-03 | Kyle's pick among card-both-now / M1-only / wait-for-both. D4's verified-against-the-repo bar reads *merged* results, and open-PR numbers can still change in review. Status lines may state that G3 decided FAIL at all three scales (that is hush-gauge PR #10's own title) with the pending-merge label stated plainly |
| D12 | Land the 2026-08-03 full-portfolio audit report (`docs/audit-2026-08-03.md`, PR #9) as the authoritative findings surface until its items are triaged into GAPS-AND-NEXT — and record that `blind-cite`'s park has ended: the repo re-published itself and resolved M1 as Option C via its own merged PR #8 (2026-08-03, verified via the GitHub API), so D6's parked state no longer holds. The portfolio's front-matter refresh for that flip is the report's #1 ranked item, pending Kyle's triage | Approved | 2026-08-03 | Audit commissioned by the 2026-08-03 handoff (deliverable: decision brief only, no project-repo changes); recording the state change and the authoritative surface was required by adversarial-review finding F3 on PR #9 |
| D13 | Card `blind-cite` as **M0 + M1 complete** with the v1-close call stated as open, not pre-decided — and refresh the portfolio's front matter for the public flip (the audit's #1 item) | Superseded by D14 | 2026-08-03 | Executes D12's #1 ranked item. Verified against blind-cite `main` at `e057c6d` (its PR #9, which landed *after* the audit's `1beb5ac` snapshot): M1 is not merely decided but complete — well-powered NULL at both surfaces, flagship blindness contrast rendered at n=2. The repo's own D11 ("v1 closes at M1") is logged **Proposed**, so the card and GAPS state it as the open call rather than reporting v1 closed. Also corrects two spots falsified by hush-gauge PR #10 merging the same day, and clears follow-ups F8 and F4 from PR #9's review |
| D14 | Re-card `blind-cite` and `hush-gauge` in full from their repos' `main`, and repair the front matter that inherited both stale stories | Approved | 2026-08-05 | Both cards had fallen behind their own repos, and D4's verified-against-the-repo bar reads *merged* results — which both now are. **`blind-cite`:** D13's "well-powered null at both surfaces" is **withdrawn**. Its M1C extension (PR #12, the repo's D24/D25, merged) ran a pre-registered power-sized extension to N=80 and found DG at **both** surfaces — stark 3/80 [1.3%, 10.5%], camouflaged 7/80 [4.3%, 17.0%] — with the flagship contrast now at 10/10 faithfulness PASS and 10/10 citation PASS. The measurement stands and its original N=20 row is kept alongside forever; the *inference* does not. D13's open v1-close call is closed by having been overtaken. **`hush-gauge`:** D11's carry-M2-as-status-only is discharged and the project is carded **complete (M0–M4)** — G0 PASS ×3, G1/G2/G3 FAIL ×3, G4 `NOT-RUN` ×3 (a passing outcome of K5's pre-committed validation ladder, not a gap), M4 gateless. Extends D10's principle: the index should not hide an active project, and it should not understate a finished one either |
